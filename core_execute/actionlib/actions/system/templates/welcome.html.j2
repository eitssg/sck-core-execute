<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to {{ company_name }}</title>
    <style>
        body { 
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif; 
            line-height: 1.6; 
            margin: 0; 
            padding: 20px; 
            background-color: #f8f9fa; 
            color: #333;
        }
        .container { 
            max-width: 600px; 
            margin: 0 auto; 
            background: white; 
            border-radius: 12px; 
            box-shadow: 0 4px 6px rgba(0,0,0,0.1); 
            overflow: hidden;
        }
        .header { 
            background: linear-gradient(135deg, #007bff 0%, #0056b3 100%); 
            color: white; 
            text-align: center; 
            padding: 40px 30px;
        }
        .header h1 { 
            margin: 0; 
            font-size: 28px; 
            font-weight: 600;
        }
        .header p { 
            margin: 10px 0 0 0; 
            opacity: 0.9; 
            font-size: 16px;
        }
        .content { 
            padding: 40px 30px;
        }
        .welcome-message { 
            font-size: 18px; 
            margin-bottom: 30px; 
            color: #2c3e50;
        }
        .info-box { 
            background: #f8f9fa; 
            border-left: 4px solid #007bff; 
            padding: 20px; 
            margin: 25px 0; 
            border-radius: 0 8px 8px 0;
        }
        .info-box h3 {
            margin-top: 0;
            color: #2c3e50;
        }
        .credentials { 
            background: #e7f3ff; 
            border: 1px solid #b3d9ff; 
            padding: 20px; 
            border-radius: 8px; 
            margin: 25px 0;
        }
        .credentials h3 { 
            margin-top: 0; 
            color: #0056b3;
        }
        .credentials code {
            background: #f8f9fa;
            padding: 2px 6px;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            color: #e83e8c;
        }
        .button { 
            display: inline-block; 
            background: linear-gradient(135deg, #007bff 0%, #0056b3 100%); 
            color: white; 
            padding: 15px 30px; 
            text-decoration: none; 
            border-radius: 8px; 
            font-weight: 600; 
            margin: 20px 0; 
            text-align: center;
            box-shadow: 0 2px 4px rgba(0,123,255,0.3);
        }
        .button:hover { 
            background: linear-gradient(135deg, #0056b3 0%, #004085 100%); 
        }
        .features { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
            gap: 20px; 
            margin: 30px 0;
        }
        .feature { 
            text-align: center; 
            padding: 20px; 
            background: #f8f9fa; 
            border-radius: 8px;
            border: 1px solid #e9ecef;
        }
        .feature-icon { 
            font-size: 24px; 
            margin-bottom: 10px;
        }
        .feature h4 {
            margin: 10px 0 5px 0;
            color: #2c3e50;
        }
        .footer { 
            background: #f8f9fa; 
            padding: 30px; 
            text-align: center; 
            border-top: 1px solid #e9ecef;
        }
        .footer p { 
            margin: 5px 0; 
            color: #6c757d; 
            font-size: 14px;
        }
        .social-links { 
            margin: 20px 0;
        }
        .social-links a { 
            color: #007bff; 
            text-decoration: none; 
            margin: 0 10px;
        }
        @media (max-width: 600px) {
            .features { 
                grid-template-columns: 1fr; 
            }
            .container { 
                margin: 10px; 
                border-radius: 8px;
            }
            .header, .content, .footer { 
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎉 Welcome to {{ company_name }}!</h1>
            <p>Your cloud automation journey starts here</p>
        </div>
        
        <div class="content">
            <div class="welcome-message">
                <p>Hello <strong>{{ user_name|title }}</strong>,</p>
                <p>We're thrilled to have you join our platform! Your account has been successfully created and you're now ready to experience the power of cloud automation.</p>
            </div>

            <div class="info-box">
                <h3>🚀 Getting Started</h3>
                <p>You can now access your dashboard and start building your cloud infrastructure. Our platform provides everything you need to deploy, manage, and scale your applications.</p>
            </div>

            {% if temp_password %}
            <div class="credentials">
                <h3>🔐 Account Information</h3>
                <p><strong>Email:</strong> {{ user_name }}</p>
                <p><strong>Temporary Password:</strong> <code>{{ temp_password }}</code></p>
                <p><em>For security reasons, we recommend changing your password after your first login.</em></p>
            </div>
            {% else %}
            <div class="info-box">
                <h3>🔐 Set Your Password</h3>
                <p>Please use the "Forgot Password" link on the login page to set your password securely.</p>
            </div>
            {% endif %}

            <div style="text-align: center;">
                <a href="{{ login_url }}" class="button">Access Your Dashboard</a>
            </div>

            {% if features %}
            <div class="features">
                {% for feature in features %}
                <div class="feature">
                    <div class="feature-icon">{{ feature.icon }}</div>
                    <h4>{{ feature.title }}</h4>
                    <p>{{ feature.description }}</p>
                </div>
                {% endfor %}
            </div>
            {% else %}
            <div class="features">
                <div class="feature">
                    <div class="feature-icon">☁️</div>
                    <h4>Cloud Deploy</h4>
                    <p>Deploy applications across multiple cloud providers with ease</p>
                </div>
                <div class="feature">
                    <div class="feature-icon">📊</div>
                    <h4>Monitor & Scale</h4>
                    <p>Real-time monitoring and auto-scaling capabilities</p>
                </div>
                <div class="feature">
                    <div class="feature-icon">🔒</div>
                    <h4>Secure by Default</h4>
                    <p>Enterprise-grade security and compliance features</p>
                </div>
                <div class="feature">
                    <div class="feature-icon">⚡</div>
                    <h4>Lightning Fast</h4>
                    <p>Optimized performance and rapid deployment cycles</p>
                </div>
            </div>
            {% endif %}

            <div class="info-box">
                <h3>📚 Next Steps</h3>
                <ul>
                    <li>Complete your profile setup</li>
                    <li>Create your first portfolio</li>
                    <li>Deploy your first application</li>
                    <li>Invite team members to collaborate</li>
                </ul>
            </div>

            <p>If you have any questions or need assistance, our support team is here to help. Don't hesitate to reach out!</p>
        </div>

        <div class="footer">
            <p><strong>Need Help?</strong></p>
            <p>Contact our support team: <a href="mailto:{{ support_email }}">{{ support_email }}</a></p>
            
            <div class="social-links">
                <a href="#">Documentation</a> | 
                <a href="#">Community</a> | 
                <a href="#">Support</a>
            </div>
            
            <p>Thank you for choosing {{ company_name }}!</p>
            <p><em>This is an automated message. Please do not reply to this email.</em></p>
        </div>
    </div>
</body>
</html>
